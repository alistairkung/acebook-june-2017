


<h1>Users#show</h1>

<p><%= @user.id %></p>

<%= link_to "New post", new_post_url(:user_id => @user.id) %>

<% if !@user.posts.empty? %>
<ul>
  <% @user.posts.reverse.each do |post| %>
 <li>  <%= post.message %> </li>
<li>  <%= post.created_at %> </li>
   <li id="user_id"> <%= post.user.id %> </li>
  <%= link_to "Like", new_like_url(:post_id => post.id) %> 
  <% end %>
</ul>
  <% end %>
